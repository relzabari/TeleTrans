# Telegram Translator

Skeleton project for translating Telegram channel messages.

## Setup
1. Install Python 3.12+
2. Run `install.bat`
3. Create Telegram API credentials at https://my.telegram.org
4. Fill `.env`
5. Edit `config.json`
6. Run `run.bat`
